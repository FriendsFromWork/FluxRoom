PORT=3001

# Comma-separated list of allowed origins for the WebSocket upgrade (CORS-equivalent).
# Leave unset to allow any origin (fine for local dev, tighten for production).
ORIGIN=https://your-app.vercel.app
