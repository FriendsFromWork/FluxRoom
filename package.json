{
  "name": "fluxroom",
  "private": true,
  "version": "1.0.0",
  "description": "Real-time peer-to-peer rooms for files, text, code, and chat.",
  "repository": {
    "type": "git",
    "url": "https://github.com/FriendsFromWork/FluxRoom.git"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=apps/server & npm run dev --workspace=apps/client",
    "build": "npm run build --workspace=packages/shared && npm run build --workspace=apps/server && npm run build --workspace=apps/client",
    "build:shared": "npm run build --workspace=packages/shared",
    "build:server": "npm run build --workspace=apps/server",
    "build:client": "npm run build --workspace=apps/client",
    "test": "vitest run",
    "lint": "npm run lint --workspace=packages/shared && npm run lint --workspace=apps/server && npm run lint --workspace=apps/client"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "vitest": "^5.0.1"
  }
}
